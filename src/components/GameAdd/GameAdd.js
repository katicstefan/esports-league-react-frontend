import React from 'react';
import PropTypes from 'prop-types';
import styles from './GameAdd.module.scss';

const GameAdd = () => (
  <div className={styles.GameAdd}>
    GameAdd Component
  </div>
);

GameAdd.propTypes = {};

GameAdd.defaultProps = {};

export default GameAdd;
